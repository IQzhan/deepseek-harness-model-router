# 变更记录

本文件记录每个版本的**用户可见变化**。版本号跟 `package/package.json` 里的一致
（由 `build-router.mjs` 写入，改版本请改构建脚本里的 `version` 字段）。

## 1.0.6

**配额类失败不再逐个模型撞墙。**

- \QUOTA\/\quota\/\illing\ 类失败是**账号配额**，不是某个模型的问题：同一个 provider 池里其余模型
  会给出完全一样的答复（实测：google 配额耗尽时，一个九模型池被逐个 429 走完）。现在这类失败直接
  走"该任务本轮无法服务"的既有级联 —— 禁用任务 → 回退默认任务 —— 而不是在同一 provider 里继续轮转。
- **普通限流不算**：\RATE_LIMIT\ 仍然逐模型轮转（按模型的限流是真实存在的，换个模型正是对的）。
- 适配层断言 228 → 233。

## 1.0.5

**修掉一个会让子会话无限重试的死循环，以及健康面读数说假话的问题。**

- **无限重试（实测 164 次）。** 真实事件载荷是 { turn, step, provider, failure, retryPolicy, signal }
  —— **没有 model**（\packages/core/agent-loop/src/agent.ts:448-457\）。旧代码从事件里读 model，于是写进
  "已耗尽"集合的键是 \google\0undefined\，永远匹配不上池里的真实条目：每个候选都永远"没试过"，
  重试永不终止。实测场景是 google 配额 429：一个子会话连续尝试 164 次、每 400ms 一次、模型来回交替
  （每次重试都轮转池子），这一轮永远结束不了。
  现在"哪条路线失败了"取自**插件自己实际下发的路由**（在改写请求头时按轮次记下），并且加了一道
  **重试预算**（池大小 + 2）：任何未预料到的失败形态都不可能再无限转下去——每一圈都是一次真实的
  provider 调用。
- **健康面读数不再说假话。** \outing.stats[].weights\ 以前报的是 smooth-WRR 的**运行累加器**
  （初始为空、赢家会被减去总数，所以会出现负数——实测一个 9 模型池打印出 \-11,1,1,…\）；
  \
ext\ 是拿**当前**累加器比较得出的，而 \pick()\ 是**先加权重再比较**，权重不等时两者不一致。
  现在报**配置的权重**、轮转实际基于的路线列表，以及**按 pick() 同样算法推出来的 next**
  （离线断言"预测的 next 必须等于实际 pick 的结果"），并且传实时任务列表进去，刚改过的池子不会被
  报成它原来的样子。
- 适配层断言 223 → 228；策略核心 90。

## 1.0.4

**让深度守卫在真实部署里真的装上；顺带修掉熔断把"降级"当成"路由坏了"的口径。**

- 1.0.3 的守卫装不上去：真实的服务实例是**冻结的**，实例属性赋值无效（插件自己的诊断抓到了这一
  点——"start is not writable"，这正是把 depthGuard 放进健康面的意义：护栏不在就明说）。
- 现在按"实例 → 原型"两级尝试：类方法本来就住在原型上，那里可写。只有当原型**自己拥有**该方法、
  且不是 `Object.prototype`/`Function.prototype` 这类共享根时才会去动它——为了管一个服务去改
  `Object.prototype` 是另一个量级的错误（测试里显式断言它没被动过）。
- 两级都失败时仍然只是记录 + 在 `/health` 显示 `depthGuard: false`，绝不假装生效。
- **熔断口径**：只把**请求路径**上的失败计入（`agent/request`）。安装失败、查询失败、过滤器被拒、
  屏蔽被拒这类都是**降级**——页面照样显示（`errors[]` 里带 `counted: false`），但不会把插件从请求
  路径里摘出去。实测：守卫装不上时每次保存设置写 2 条，**3 次保存就能凑满阈值 5**，把路由整整停
  60 秒——一个配置问题不该造成停摆。
- 适配层断言 218 → 223。

## 1.0.3

**修掉"子智能体不会再分发"这个说错的承诺，以及另外三个真实缺陷。**

- **递归真的发生过。** 实测：`childDelegation: false` 时，子会话调用 `subagent` **成功创建了一个
  `delegationDepth: 2` 的孙子会话**。原因：子会话跑的是父预设的副本，那份副本把内置委派工具装进了
  **子会话自己的作用域**；自有层注册的名字外部屏蔽不掉（`restrict` 报 "unknown global tool"，
  而且会让整个过滤器失效——白名单也留不住它：实测子会话工具表 33→32，`subagent` 仍在）。
  `maxDepth` 只约束本插件自己发出的启动请求，管不住内置工具。
  **现在深度策略下沉到 `ctx.subagents`**：包住 `start`/`startContinuable`，调用者深度达预算即拒绝；
  预算读实时配置（关 = 1 层，开 = 2 层）；随 fiber 撤销、插件关闭后不留痕迹；装不上会进 `errors[]`
  并在 `/health` 的 `delegation.depthGuard` 显示为未生效。
- **失败重试会重新问分类器，于是同一轮里任务会变。** 实测：某子会话第一次派发落到默认任务，
  模型报错后重试却换了另一个任务——根因是重试分支删掉了本轮的分类决策，而分类器是 LLM，
  同一个请求两次可以判得不一样。现在重试只轮转池子，分类结果在整轮内保持不变。
- **任务声明 `childTools` 会丢掉默认白名单。** 声明 `deny` 时插件不再发送默认白名单，于是继承来的
  控制器工具（`send_message`/`list_agents`）又回到子会话手里。现在 `deny` 是**折进**白名单，而不是
  替换它。
- **屏蔽不掉的委派工具名会被静默吞掉。** 现在记进 `/health` 的 `agents[].unmasked`——挡不住什么
  一眼可见，而不是让人以为它挡住了。
- **委派工具描述与实际行为不一致。** 分类器关闭时描述仍在说"会按语义分类"；现在按实际状态给出三种
  说法（分类 / 走默认任务 / 保持继承），不再承诺做不到的事。
- 适配层断言 180 → 212。

## 1.0.2

**让 `reasoningEffort` 真的生效，并让 revision 只随内容变。**

- **推理强度改为"就近映射"**。DSH 拿模型能力校验请求且**拒绝夹取**：模型没有的档位直接抛
  `UNSUPPORTED_REASONING_EFFORT`（实测 b-ai 的 `qwen3.8-flash` 遇到 `max` 当场终止子会话）。原先
  任务声明了强度只有两种结局，而两种都不对——**原样发出去**（不支持就整轮死掉）或**路由换模型时被
  丢掉**（你以为在省钱，其实在付模型默认档）。现在通过 `llm.resolveModelInfo()` 查一次目标模型的档位
  （按路由缓存），支持就原样保留；不支持就沿 `max → high → medium → low → off` 向下取最近一档；
  低于模型底线则取它自己的最低档；完全不支持推理才不发该字段；查不到（老版本 `llm`）则照声明原样发，
  由适配器报真实的错。
- **revision 改为按内容计算**。以前它来自文件签名（`size:mtime`），所以**只是碰一下文件**（哪怕原样
  写回同样的字节）也会变，设置页会把一份完全有效的草稿判成"配置已被其他来源修改"。现在它哈希
  **生效中的配置内容 + 解析问题**：内容没变 revision 就不动，内容变了（或某个文件解析失败）才动。
  栅栏的保护力不变，误报消失。
- **测试不再改写真实配置**。接线套件以前会手改部署目录里的 `tasks/general.yml`，并把当前配置 POST 回
  真实目录；现在真实目录**只读**，所有写入用例跑在同一份目录的**临时副本**上，最后一条断言是
  "跑完这一套，线上 revision 没有动过"。
- 适配层断言 167 → 180，接线套件 14 → 17。

## 1.0.1

**修掉一个"看起来没装上、其实一直在工作"的缺陷。**

- **现象**：`/health` 报 `installed: 0`、`owned: false`，可是 `subagent` 工具明明在会话的工具表里
  （它的描述里带着你配置的任务清单），路由也确实在跑。
- **原因**：Cordis **异步**启动插件——`ctx.plugin()` 返回时 `apply` 还没执行。旧代码用
  "`apply` 跑过没有"当返回条件（`built ? fiber : undefined`），于是把一个**已经注册进 agent
  作用域的活安装**丢掉了。
- **后果**：不只是数字不对。没有句柄 = **撤不掉**：撤销授权、关闭插件、强制重建都 dispose
  不到它，重建还会在同一作用域叠出第二份注册。"撤销即还原"的承诺当场失效。
- **修法**：fiber **创建即登记**；新增 `delegation.applied` 记录启动是否真的跑过，`delegation.service`
  记录委派服务在不在作用域里，`agents[].error` 记录那次启动抛出的原因（启动是延迟执行的，
  调用方的 try/catch 看不到）。启动期抛错**就地 teardown 再上抛**，不会留下"半个安装"。
- **设置页**：运行状态改成「委派工具已生效 N / 已挂载 M 个会话」——两个数相等才代表接管真的生效。
- **测试**：测试替身 `fakeAgent` 现在也**延迟**启动（`queueMicrotask` + `startupGate`），
  所以"同步假设"在离线套件里就会红。新增 4 组回归（延迟启动期间的所有权、启动失败的上报与
  原子回滚、没有委派服务时的原因可见、子智能体启动失败的遮蔽回滚），适配层断言 141 → 167。

## 1.0.0

首个可用版本。

**按任务选模型**

- 判定阶梯：显式 `[task: 任务id]` → 关键词 → 语义分类器（每轮一次、结果缓存）→ 默认任务 →
  保持子会话继承的模型。
- 模型池按权重**全局**轮转（所有会话共享一个轮转，不按会话分片），一轮内钉住同一个模型，
  保住 prompt 前缀缓存。
- 失败三级降级：池内换下一个模型 → 整个池失败则改用默认任务 → 都不行则不再改写路由。

**零预设改动地提供委派能力**

- 插件在运行时把自己的 `subagent`（派一个闭包任务）与 `subagent_message`（把修改意见发回
  **同一个**子智能体）注册进被授权预设的 agent 作用域，并屏蔽同作用域里的内置委派工具。
- 子智能体默认**不可能**再往下派：只装给非子智能体 + 深度上限 + 执行期拒绝。是否允许再细分
  只由 `childDelegation` 决定，与预设无关。
- 撤销授权即销毁注册；**没有任何预设文件被改写过**。

**每个任务可以塑造自己的子智能体**

- `childPersona`：整体替换继承来的系统提示（作用域同名遮蔽 + complete）。
- `childTools`：`{ allow }` 或 `{ deny }`，限定子智能体可用工具；勾选界面从 Host 的真实工具表生成。
- `reasoningEffort`：覆盖路由默认的推理强度，避免廉价池每步都在付 `high` 的推理成本。

**配置是文件**

- 配置移出 `settings.yaml`，改为 `$DSH_HOME/model-routing/`：`global.yml` + 每个任务一个文件。
- 首次启动自动从旧的 `model-routing:` 段迁移，并把那一段从 `settings.yaml` 删除。
- 目录轮询（1 秒）让手工编辑即时生效；写入走临时文件 + 重命名，读方永远看不到半截文件；
  某个文件解析失败只影响它自己，其余照常加载。
- 设置页与文件是同一份数据：草稿 + 一次原子提交 + revision 栅栏（别处改过就拒绝覆盖）。

**设置页**

- 输入期间**不落盘**（光标不再跳到末尾），顶部常驻保存栏显示"改了哪些字段"，可保存或取消。
- 子智能体档案、推理强度、工具勾选都是**点选**控件；工具清单来自 Host 的真实工具表。
- 运行状态卡片：配置来源与目录、能力探测、最近 Host 错误、每个 provider 的失败次数、
  以及插件是否已自动停用。

**可靠性与可观测性**

- 连续 5 次失败自动把自己从请求路径里摘出去（60 秒后自动重试，改动设置立即重置）。
- 设置页面对敌意环境有结构性防护：faces 解析永不抛、镜像读取是全函数、错误边界兜底、
  边界类惰性创建（不会在 React 不完整时让整个 bundle 加载失败）。
- Host 提供 `GET /api/dsh-model-router/health` 与 `GET|POST /api/dsh-model-router/config`。
  其中"接口没回应"本身就被显示为"Host 半边可能没有挂载"——这是进程内上报无法覆盖的故障。

**随插件附带**

- `skills/model-routing/SKILL.md`：只能 `/model-routing` 主动调用的配置助手
  （`disable-model-invocation: true`，不进模型目录）。
- `node build-router.mjs` 同时完成构建、link 进 profile、安装/刷新 skill。

**测试**：8 个套件、近 500 条断言，覆盖策略核心、配置文件（真实文件系统）、适配层、
构建产物接线、两个真实加载器、设置页真实 React 渲染、本机真实配置，以及真实联网调用。
