{
  "name": "dsh-model-router-repo",
  "version": "1.0.0",
  "private": true,
  "description": "Source repository for the dsh-model-router plugin. The installable package is built into package/.",
  "type": "module",
  "scripts": {
    "build": "node build-router.mjs",
    "test": "node run-tests.mjs",
    "test:live": "node test-live-nondeepseek.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
